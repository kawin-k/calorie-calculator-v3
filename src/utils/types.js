export const RoomTypes = Object.freeze({
  NORMAL: 'normal',
  SPECIAL: 'special',
})

export const PatientTypes = Object.freeze({
  SPECIALIZE: 'specialize',
  DIABETES: 'diabetes',
})

export const FoodTypes = Object.freeze({
  REGULAR: 'regular',
  SOFT: 'soft',
})

export const FoodTime = Object.freeze({
  MORNING: 'morning',
  NOON: 'noon',
  EVENING: 'evening',
})